<%
  size = media_size_for_version(version)
%>
<span class='previewMedia'>
<%=
  image_tag plugin_model.media_file.thumbnail.thumb(size[:area]).url if (defined? plugin_model.media_file.thumbnail.stored? && plugin_model.media_file.thumbnail.stored?)
%>
<%
	media_id = plugin_model.media_file.id
	if !(version.to_s == "icon" || version.to_s == "thumbnail" || caption == 'no')
%>
  		
  		<!-- This renders the Media type/source thumbnail along with a popover guide to the icons -->
		<div class="previewMediaCaption row">
			<div class="col-xs-12 previewMediaCol text-left idle-fade">
				<div class="mediaTips">
					<span class="popoverWrapper infoIcon" title="Flickr Image"><div class="previewMediaIcon"><i class="fa fa-image" aria-hidden="true"></i>&nbsp;&nbsp;<i class="fa fa-flickr" aria-hidden="true"></i></div></span>
				</div>
				<%= sanitize(plugin_model.media_file.caption) %>
			</div>
	  	</div>
	  	<!-- End Media guide and thumbnail section -->
<%
	end
%>
</span>
