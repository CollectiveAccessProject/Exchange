<%
  if (size = media_size_for_version(version))

    end

case version
  when :medium
    url = 'http://img.youtube.com/vi/' + plugin_model.key + '/mqdefault.jpg'
  when :full
    url = 'http://img.youtube.com/vi/' + plugin_model.key + '/maxresdefault.jpg'
  else
    url = 'http://img.youtube.com/vi/' + plugin_model.key + '/default.jpg'
end

%>

<img
  src="<%= url %>"
  <% if width %> width="<%= width %>" <% end %>
  <% if height %> height="<%= height %>" <% end %>
/>
