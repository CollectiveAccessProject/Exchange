<div class="row featuredTop">
  <div class="col-md-12">
    <h2><%= set.title %> </h2>
    <% if (set.subtitle.chop.length > 0) %>
        <h3><%= set.subtitle %></h3>
    <% end %>
    <% if (set.body_text.chop.length > 0) %>
        <h4><%= set.body_text %></h4>
    <% end %>
  </div>
</div>
<div class="row">
<%
	i = 0
%>
  <% items.each do |item| %>
<%
		i = i+1;
%>
      <div class="col-sm-4 featuredItemCol">
      	<h3><%= (item.title.chop.length > 0) ? item.title : item.resource.title %></H3>
        <%= link_to(item.resource.media_files.first.sourceable.preview(:medium), resource_path(item.resource_id)) %>
        <p><%= item.subtitle %></p>
      </div>
<%
		if ((i % 3) == 0)
%>
			</div><div class="row">
<%
		end
%>
  <% end %>
</div>