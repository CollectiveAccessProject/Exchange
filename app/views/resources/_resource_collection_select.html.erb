<div id="selectResourceCollectionForm" class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="addMediaModal" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <h2 class="modal-title" id="addMediaFormLabel">Add to a learning collection</h2>
      </div>
      <div class="modal-body">
        <div class="row">
          <div class="col-md-12">
            Learning Collections are groups of Resources (and/or sub-Collections) with a common theme. You can use Learning
            Collections to make slideshow, tours, presentations, digital papers and more.
          </div>
        </div>
        <div class="row" style="margin-top: 30px;">
          <div class="col-md-12 text-center">
            <strong>Would you like to organize this <%= (@resource.is_resource) ? "Resource" : "Collection" %> within a Learning Collection?</strong>
          </div>
        </div>
        <div class="row" style="margin-top: 30px;">
          <div class="col-sm-6 col-md-5 col-md-offset-1 text-center">
            <%= link_to "Create new Learning Collection", new_collection_with_id_path(@resource.id), {"class": "btn btn-default"} %>
          </div>
          <div class="col-sm-6 col-md-5 text-center">
          <% if (@available_collections.length > 0) %>
          	<div class="row">
				<%= form_tag(set_resource_parent_with_id_path(@resource), method: "post") do %>
					<div class="col-sm-3 text-right"><small>or use:</small></div>
					<div class="col-sm-7">
					<%=
					  select_tag(:parent_id, options_from_collection_for_select(@available_collections, :id, lambda {|t| strip_tags(t.title)}), class:'form-control')
					%>
					<script>
						$(document).ready(function(){
							var option_str = $('#parent_id option').text();
							console.log($(option_str).text());
						});
					</script>
					<%= hidden_field_tag(:id, @resource.id) %>
					</div>
					<div class="col-sm-2">
					<%=
					  submit_tag("Go", "class": "btn btn-default", "id": "addToExistingCollection")
					%>
					</div>
				<% end %>
			</div>
            <% end %>
          </div>
        </div>
        <div class="row" style="margin-top: 20px">

          <div class="col-md-12 text-right">
            <br/><a class="" data-dismiss="modal" aria-label="Close">No thanks <i class="fa fa-times"></i></a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
